import { colors } from "../constants/colors";

export const theme = {
  colors,
  spacing: {
    xs: 6,
    sm: 10,
    md: 16,
    lg: 20,
    xl: 24,
    xxl: 32,
  },
  radius: {
    md: 16,
    lg: 24,
    xl: 32,
  },
} as const;
